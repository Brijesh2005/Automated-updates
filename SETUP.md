# 1.Change username and email to your details in daily.yml
# 2.Get your GitHub auth token and go to repo settings -> Secrets and variables -> Actions -> New repository secrets -> GH_AT then your GitHub token.
# 3.Save it.
# 4.To verify go to actions -> run workflow from main branch
# 5.It will automatically update itself once every 6 hours.
